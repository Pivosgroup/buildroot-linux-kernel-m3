#
# Input core configuration
#



config AM_IR_RECEIVER
	tristate "Amlogic IR Receiver(Self-study Remote) support"
	default n
	help
	  Say Y here if you want to use the amlogic self-study remote function.

	  To compile this driver as a module, choose M here: the
	  module will be called .


